{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window-overlay",
  "description": "Allow the main window to use overlay title bar drag regions.",
  "windows": ["main"],
  "webviews": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
